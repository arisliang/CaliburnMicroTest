<UserControl x:Class="CaliburnMicroTest.WpfUi.Views.ColorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="Blue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <RadioButton Grid.Row="0" Name="Red" VerticalAlignment="Center" HorizontalAlignment="Center">
            <WrapPanel Orientation="Horizontal">
                <TextBlock Text="Red" Foreground="White"/>
            </WrapPanel>
        </RadioButton>
        <RadioButton Grid.Row="1" Name="Green" VerticalAlignment="Center" HorizontalAlignment="Center">
            <WrapPanel Orientation="Horizontal">
                <TextBlock Text="Green" Foreground="White"/>
            </WrapPanel>
        </RadioButton>
        <RadioButton Grid.Row="2" Name="Blue" VerticalAlignment="Center" HorizontalAlignment="Center">
            <WrapPanel Orientation="Horizontal">
                <TextBlock Text="Blue" Foreground="White"/>
            </WrapPanel>
        </RadioButton>
    </Grid>
</UserControl>
